# TODO could switch to a systemd service and timer and `loginctl enable-linger`

MAILTO=""

* * * * * flock -n "$HOME/src/panboticon-skills/bin/poll-github" "$HOME/src/panboticon-skills/bin/poll-github" 2>&1 | logger -t poll-github
